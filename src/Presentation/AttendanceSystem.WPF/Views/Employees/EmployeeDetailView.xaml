<UserControl x:Class="AttendanceSystem.WPF.Views.Employees.EmployeeDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource MahApps.Styles.TextBox}">
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
        <Style TargetType="ComboBox" BasedOn="{StaticResource MahApps.Styles.ComboBox}">
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
        <Style TargetType="DatePicker" BasedOn="{StaticResource MahApps.Styles.DatePicker}">
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
        <Style TargetType="GroupBox" BasedOn="{StaticResource MahApps.Styles.GroupBox}">
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Title -->
            <RowDefinition Height="*"/>    <!-- ScrollViewer -->
            <RowDefinition Height="Auto"/> <!-- Buttons -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Form Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
            <StackPanel>
                
                <!-- Datos Personales -->
                <GroupBox Header="Datos Personales">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Row 0 -->
                        <StackPanel Grid.Row="0" Grid.Column="0">
                            <TextBlock Text="No. Empleado"/>
                            <TextBox Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}" 
                                     IsEnabled="{Binding IsEditMode, Converter={StaticResource InverseBooleanConverter}}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="0" Grid.Column="2">
                            <TextBlock Text="Fecha de Contratación"/>
                            <DatePicker SelectedDate="{Binding HireDate}"/>
                        </StackPanel>

                        <!-- Row 1 -->
                        <StackPanel Grid.Row="1" Grid.Column="0">
                            <TextBlock Text="Nombre(s)"/>
                            <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="2">
                            <TextBlock Text="Apellidos"/>
                            <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>

                        <!-- Row 2 -->
                        <StackPanel Grid.Row="2" Grid.Column="0">
                            <TextBlock Text="Correo Electrónico"/>
                            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="2" Grid.Column="2">
                            <TextBlock Text="Teléfono"/>
                            <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>

                        <!-- Row 3 -->
                        <StackPanel Grid.Row="3" Grid.Column="0">
                            <TextBlock Text="Género"/>
                            <ComboBox ItemsSource="{Binding GenderOptions}" SelectedItem="{Binding Gender}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="3" Grid.Column="2">
                            <TextBlock Text="Estado" Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            <ComboBox ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding Status}"
                                      Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <!-- Datos Laborales -->
                <GroupBox Header="Información Laboral">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Row 0 -->
                        <StackPanel Grid.Row="0" Grid.Column="0">
                            <TextBlock Text="Sucursal"/>
                            <ComboBox ItemsSource="{Binding Branches}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding SelectedBranchId}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="0" Grid.Column="2">
                            <TextBlock Text="Departamento"/>
                            <ComboBox ItemsSource="{Binding Departments}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding SelectedDepartmentId}"/>
                        </StackPanel>

                        <!-- Row 1 -->
                        <StackPanel Grid.Row="1" Grid.Column="0">
                            <TextBlock Text="Puesto"/>
                            <ComboBox ItemsSource="{Binding VisiblePositions}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding SelectedPositionId}"/>
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <!-- Horario y Turno -->
                <GroupBox Header="Asignación de Horario">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Row 0 -->
                        <StackPanel Grid.Row="0" Grid.Column="0">
                            <TextBlock Text="Tipo de Turno"/>
                            <ComboBox ItemsSource="{Binding ShiftTypeOptions}" SelectedItem="{Binding SelectedShiftType}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="0" Grid.Column="2">
                            <TextBlock Text="Horario"/>
                            <ComboBox ItemsSource="{Binding VisibleShifts}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding SelectedShiftId}"/>
                        </StackPanel>

                        <!-- Row 1 -->
                        <StackPanel Grid.Row="1" Grid.Column="0">
                            <TextBlock Text="Día de Descanso"/>
                            <ComboBox ItemsSource="{Binding DaysOfWeek}" DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding SelectedRestDay}"/>
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <!-- Horas Extra -->
                <GroupBox Header="Configuración de Horas Extra">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Row 0 -->
                        <CheckBox Grid.Row="0" Grid.Column="0" IsChecked="{Binding OvertimeAuthorized}" Content="Autorizar Horas Extra" Margin="0,0,0,15"/>

                        <!-- Row 1 -->
                        <StackPanel Grid.Row="1" Grid.Column="0" IsEnabled="{Binding OvertimeAuthorized}">
                            <TextBlock Text="Método de Cálculo"/>
                            <ComboBox ItemsSource="{Binding OvertimeCalculationMethodOptions}" SelectedItem="{Binding SelectedOvertimeCalculationMethod}"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="2" IsEnabled="{Binding OvertimeAuthorized}">
                            <TextBlock Text="Tipo de Tope"/>
                            <ComboBox ItemsSource="{Binding OvertimeCapTypeOptions}" SelectedItem="{Binding SelectedOvertimeCapType}"/>
                            
                            <TextBlock Text="Minutos Máximos" Margin="0,10,0,5"/>
                            <mah:NumericUpDown Value="{Binding OvertimeCapMinutes}" Minimum="0" StringFormat="N0" Interval="1"/>
                        </StackPanel>
                    </Grid>
                </GroupBox>

            </StackPanel>
        </ScrollViewer>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Command="{Binding SaveCommand}" Style="{StaticResource MahApps.Styles.Button.Square.Accent}" Content="Guardar" Width="120" Height="35" Margin="0,0,10,0"/>
            <Button Command="{Binding CancelCommand}" Style="{StaticResource MahApps.Styles.Button.Square}" Content="Cancelar" Width="120" Height="35"/>
        </StackPanel>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="3" Background="#80000000" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <mah:ProgressRing IsActive="{Binding IsBusy}" Width="60" Height="60" Foreground="White" Margin="0,0,0,15"/>
                <TextBlock Text="{Binding BusyMessage}" Foreground="White" HorizontalAlignment="Center" FontSize="18" FontWeight="SemiBold"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
