<UserControl x:Class="AttendanceSystem.WPF.Views.Employees.EmployeeDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000"
             Background="#F5F7FA">

    <UserControl.Resources>
        <!-- Shadow Effect -->
        <DropShadowEffect x:Key="CardShadow" 
                          BlurRadius="15" 
                          ShadowDepth="2" 
                          Opacity="0.1" 
                          Color="Black"/>

        <Style x:Key="SectionCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#4E5D78"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource MahApps.Styles.TextBox}">
            <Setter Property="Padding" Value="10,12"/>
            <Setter Property="BorderBrush" Value="#EAECF4"/>
            <Setter Property="Background" Value="#F8F9FC"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource MahApps.Styles.ComboBox}">
            <Setter Property="Padding" Value="10,10"/>
            <Setter Property="BorderBrush" Value="#EAECF4"/>
            <Setter Property="Background" Value="#F8F9FC"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Content -->
            <RowDefinition Height="Auto"/> <!-- Footer Buttons -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource MahApps.Styles.Button.Circle}"
                    Width="40"
                    Height="40"
                    Margin="0,0,15,0"
                    Background="White"
                    Effect="{StaticResource CardShadow}">
                <iconPacks:PackIconMaterial Kind="ArrowLeft" Width="20" Height="20" Foreground="#4E73DF"/>
            </Button>

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="{Binding Title}" 
                           FontSize="28" 
                           FontWeight="Bold"
                           Foreground="#4E5D78"/>
                <TextBlock Text="Gestione la información detallada del colaborador"
                           FontSize="14"
                           Foreground="#858796"/>
            </StackPanel>
        </Grid>

        <!-- Main Form Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,15,0">
            <StackPanel MaxWidth="1200" HorizontalAlignment="Center">
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="25"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left Column -->
                    <StackPanel Grid.Column="0">
                        <!-- Personal Info Card -->
                        <Border Style="{StaticResource SectionCardStyle}">
                            <StackPanel>
                                <TextBlock Text="Información Personal" FontSize="18" FontWeight="Bold" Foreground="#4E73DF" Margin="0,0,0,20"/>
                                
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="20"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Photo Placeholder -->
                                    <Border Grid.Column="0" Width="100" Height="100" CornerRadius="50" Background="#F8F9FC" BorderBrush="#EAECF4" BorderThickness="2" VerticalAlignment="Top">
                                        <iconPacks:PackIconMaterial Kind="Account" Width="40" Height="40" Foreground="#D1D3E2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="ID de Empleado" Style="{StaticResource FieldLabelStyle}"/>
                                        <TextBox Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}" 
                                                 Style="{StaticResource ModernTextBoxStyle}"
                                                 IsEnabled="{Binding IsEditMode, Converter={StaticResource InverseBooleanConverter}}"/>
                                        
                                        <TextBlock Text="Fecha de Contratación" Style="{StaticResource FieldLabelStyle}"/>
                                        <DatePicker SelectedDate="{Binding HireDate}" Margin="0,0,0,15"/>
                                    </StackPanel>
                                </Grid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="15"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Nombre(s)" Style="{StaticResource FieldLabelStyle}"/>
                                        <TextBox Text="{Binding FirstName}" Style="{StaticResource ModernTextBoxStyle}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Apellidos" Style="{StaticResource FieldLabelStyle}"/>
                                        <TextBox Text="{Binding LastName}" Style="{StaticResource ModernTextBoxStyle}"/>
                                    </StackPanel>
                                </Grid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="15"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Sexo" Style="{StaticResource FieldLabelStyle}"/>
                                        <ComboBox ItemsSource="{Binding GenderOptions}" SelectedItem="{Binding Gender}" Style="{StaticResource ModernComboBoxStyle}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Estatus" Style="{StaticResource FieldLabelStyle}"/>
                                        <ComboBox ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding Status}" Style="{StaticResource ModernComboBoxStyle}"
                                                  IsEnabled="{Binding IsEditMode}"/>
                                    </StackPanel>
                                </Grid>

                                <TextBlock Text="Correo Electrónico" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding Email}" Style="{StaticResource ModernTextBoxStyle}"/>

                                <TextBlock Text="Teléfono" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding PhoneNumber}" Style="{StaticResource ModernTextBoxStyle}"/>
                            </StackPanel>
                        </Border>

                        <!-- Labor Info Card -->
                        <Border Style="{StaticResource SectionCardStyle}">
                            <StackPanel>
                                <TextBlock Text="Estructura Organizacional" FontSize="18" FontWeight="Bold" Foreground="#4E73DF" Margin="0,0,0,20"/>
                                
                                <TextBlock Text="Sucursal" Style="{StaticResource FieldLabelStyle}"/>
                                <ComboBox ItemsSource="{Binding Branches}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding SelectedBranchId}" Style="{StaticResource ModernComboBoxStyle}"/>

                                <TextBlock Text="Departamento" Style="{StaticResource FieldLabelStyle}"/>
                                <ComboBox ItemsSource="{Binding Departments}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding SelectedDepartmentId}" Style="{StaticResource ModernComboBoxStyle}"/>

                                <TextBlock Text="Puesto" Style="{StaticResource FieldLabelStyle}"/>
                                <ComboBox ItemsSource="{Binding VisiblePositions}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding SelectedPositionId}" Style="{StaticResource ModernComboBoxStyle}"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Right Column -->
                    <StackPanel Grid.Column="2">
                        <!-- Schedule Info Card -->
                        <Border Style="{StaticResource SectionCardStyle}">
                            <StackPanel>
                                <TextBlock Text="Horario y Jornada" FontSize="18" FontWeight="Bold" Foreground="#4E73DF" Margin="0,0,0,20"/>
                                
                                <TextBlock Text="Tipo de Turno" Style="{StaticResource FieldLabelStyle}"/>
                                <ComboBox ItemsSource="{Binding ShiftTypeOptions}" SelectedItem="{Binding SelectedShiftType}" Style="{StaticResource ModernComboBoxStyle}"/>

                                <TextBlock Text="Horario Asignado" Style="{StaticResource FieldLabelStyle}"/>
                                <ComboBox ItemsSource="{Binding VisibleShifts}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding SelectedShiftId}" Style="{StaticResource ModernComboBoxStyle}"/>

                                <TextBlock Text="Día de Descanso" Style="{StaticResource FieldLabelStyle}"/>
                                <ComboBox ItemsSource="{Binding DaysOfWeek}" DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding SelectedRestDay}" Style="{StaticResource ModernComboBoxStyle}"/>
                            </StackPanel>
                        </Border>

                        <!-- Device Config Card -->
                        <Border Style="{StaticResource SectionCardStyle}">
                            <StackPanel>
                                <TextBlock Text="Configuración de Dispositivo" FontSize="18" FontWeight="Bold" Foreground="#4E73DF" Margin="0,0,0,20"/>
                                
                                <TextBlock Text="Número de Tarjeta" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding CardNumber}" Style="{StaticResource ModernTextBoxStyle}" mah:TextBoxHelper.Watermark="Ej: 12345678"/>

                                <TextBlock Text="Contraseña PIN" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Text="{Binding DevicePassword}" Style="{StaticResource ModernTextBoxStyle}" mah:TextBoxHelper.Watermark="Máx. 8 dígitos"/>
                                
                                <TextBlock Text="Biometría Registrada" Style="{StaticResource FieldLabelStyle}" Margin="0,10,0,10"/>
                                <StackPanel Orientation="Horizontal">
                                    <Border Background="#EBFBEE" CornerRadius="15" Padding="12,6" Margin="0,0,10,0">
                                        <StackPanel Orientation="Horizontal">
                                            <iconPacks:PackIconMaterial Kind="Fingerprint" Width="14" Height="14" Foreground="#28A745" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="2 Huellas" FontSize="12" Foreground="#28A745" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Background="#F8F9FC" CornerRadius="15" Padding="12,6">
                                        <StackPanel Orientation="Horizontal">
                                            <iconPacks:PackIconMaterial Kind="FaceRecognition" Width="14" Height="14" Foreground="#858796" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="Sin Rostro" FontSize="12" Foreground="#858796" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Overtime Config Card -->
                        <Border Style="{StaticResource SectionCardStyle}">
                            <StackPanel>
                                <TextBlock Text="Configuración de Tiempo Extra" FontSize="18" FontWeight="Bold" Foreground="#4E73DF" Margin="0,0,0,20"/>
                                
                                <CheckBox IsChecked="{Binding OvertimeAuthorized}" Content="Autorizar Tiempo Extra" Margin="0,0,0,10" FontWeight="SemiBold" Foreground="#4E5D78"/>
                                <CheckBox IsChecked="{Binding CalculateOvertimeBeforeEntry}" Content="Calcular antes de la entrada" Margin="0,0,0,20" IsEnabled="{Binding OvertimeAuthorized}" Foreground="#858796"/>

                                <StackPanel IsEnabled="{Binding OvertimeAuthorized}">
                                    <TextBlock Text="Método de Redondeo" Style="{StaticResource FieldLabelStyle}"/>
                                    <ComboBox ItemsSource="{Binding OvertimeCalculationMethodOptions}" SelectedItem="{Binding SelectedOvertimeCalculationMethod}" Style="{StaticResource ModernComboBoxStyle}"/>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="15"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Tipo de Tope" Style="{StaticResource FieldLabelStyle}"/>
                                            <ComboBox ItemsSource="{Binding OvertimeCapTypeOptions}" SelectedItem="{Binding SelectedOvertimeCapType}" Style="{StaticResource ModernComboBoxStyle}"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Minutos Tope" Style="{StaticResource FieldLabelStyle}"/>
                                            <mah:NumericUpDown Value="{Binding OvertimeCapMinutes}" Minimum="0" StringFormat="N0" Interval="1" Height="40" Background="#F8F9FC" BorderBrush="#EAECF4"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
                
                <Rectangle Height="40" Fill="Transparent"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer Actions -->
        <Border Grid.Row="2" Background="White" Margin="-30,0" Padding="30,20" BorderBrush="#EAECF4" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Command="{Binding CancelCommand}" 
                        Content="Cancelar" 
                        Width="140" 
                        Height="45" 
                        Background="#F8F9FC"
                        Foreground="#858796"
                        BorderBrush="#EAECF4"
                        Margin="0,0,15,0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button Command="{Binding SaveCommand}" 
                        Content="Guardar Colaborador" 
                        Width="200" 
                        Height="45" 
                        Background="#4E73DF"
                        Foreground="White"
                        FontWeight="Bold"
                        BorderThickness="0"
                        Effect="{StaticResource CardShadow}">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="3" Background="#B3000000" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="-30">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <mah:ProgressRing IsActive="{Binding IsBusy}" Width="60" Height="60" Foreground="White" Margin="0,0,0,20"/>
                <TextBlock Text="{Binding BusyMessage}" Foreground="White" HorizontalAlignment="Center" FontSize="18" FontWeight="Bold"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
