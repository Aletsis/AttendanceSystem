<UserControl x:Class="AttendanceSystem.WPF.Views.Devices.DeviceDetailDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             Width="450">
    
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterOwner" />
            <Setter Property="ResizeMode" Value="NoResize"/>
            <Setter Property="ShowInTaskbar" Value="False"/>
            <Setter Property="SizeToContent" Value="Height"/>
            <Setter Property="WindowStyle" Value="None"/>
            <Setter Property="AllowsTransparency" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
    </prism:Dialog.WindowStyle>

    <Border Background="White" CornerRadius="15" BorderThickness="1" BorderBrush="#EAECF4">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.1" Color="Black"/>
        </Border.Effect>
        
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="{Binding Title}" FontSize="22" FontWeight="Bold" Foreground="#4E5D78"/>
                <TextBlock Text="Configure los parámetros de conexión del reloj checador" FontSize="13" Foreground="#858796" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Form -->
            <ScrollViewer Grid.Row="1" MaxHeight="550" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="Nombre del Dispositivo" FontWeight="SemiBold" Foreground="#4E5D78" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                                 mah:TextBoxHelper.Watermark="Ej: Entrada Principal"
                                 Padding="10,8"/>
                    </StackPanel>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,10,0">
                            <TextBlock Text="Dirección IP" FontWeight="SemiBold" Foreground="#4E5D78" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding IpAddress, UpdateSourceTrigger=PropertyChanged}" 
                                     mah:TextBoxHelper.Watermark="192.168.1.100"
                                     Padding="10,8"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Puerto" FontWeight="SemiBold" Foreground="#4E5D78" Margin="0,0,0,5"/>
                            <mah:NumericUpDown Value="{Binding Port}" Minimum="1" Maximum="65535" Padding="5"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="Ubicación / Sucursal" FontWeight="SemiBold" Foreground="#4E5D78" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Location, UpdateSourceTrigger=PropertyChanged}" 
                                 mah:TextBoxHelper.Watermark="Ej: Sucursal Centro"
                                 Padding="10,8"/>
                    </StackPanel>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,10,0">
                            <TextBlock Text="Marca" FontWeight="SemiBold" Foreground="#4E5D78" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding Brands}" 
                                      SelectedItem="{Binding SelectedBrand}"
                                      Padding="10,5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Método de Descarga" FontWeight="SemiBold" Foreground="#4E5D78" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding DownloadMethods}" 
                                      SelectedItem="{Binding SelectedDownloadMethod}"
                                      Padding="10,5"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="Número de Serie" FontWeight="SemiBold" Foreground="#4E5D78" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding SerialNumber, UpdateSourceTrigger=PropertyChanged}" 
                                 mah:TextBoxHelper.Watermark="Requerido para ADMS (Push)"
                                 Padding="10,8"/>
                    </StackPanel>

                    <mah:ToggleSwitch Header="Borrar registros tras descarga" 
                                      IsOn="{Binding ShouldClearAfterDownload}"
                                      Margin="0,0,0,15"/>

                    <Border BorderThickness="0,1,0,0" BorderBrush="#EAECF4" Margin="0,5,0,15"/>
                    <TextBlock Text="Credenciales (Opcional)" FontWeight="Bold" Foreground="#4E5D78" Margin="0,0,0,10"/>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,10,0">
                            <TextBlock Text="Usuario" FontWeight="SemiBold" Foreground="#4E5D78" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                                     mah:TextBoxHelper.Watermark="admin"
                                     Padding="10,8"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Contraseña" FontWeight="SemiBold" Foreground="#4E5D78" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"
                                     mah:TextBoxHelper.Watermark="••••••••"
                                     Padding="10,8"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </ScrollViewer>

            <!-- Actions -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Cancelar" 
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource MahApps.Styles.Button.Flat}"
                        Margin="0,0,10,0"
                        Padding="20,10"/>
                
                <Button Content="Guardar" 
                        Command="{Binding SaveCommand}"
                        Background="#4E73DF"
                        Foreground="White"
                        Padding="30,10"
                        BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
