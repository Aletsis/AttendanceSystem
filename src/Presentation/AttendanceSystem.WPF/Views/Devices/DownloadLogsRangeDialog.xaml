<UserControl x:Class="AttendanceSystem.WPF.Views.Devices.DownloadLogsRangeDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             Width="350">
    
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterOwner" />
            <Setter Property="ResizeMode" Value="NoResize"/>
            <Setter Property="ShowInTaskbar" Value="False"/>
            <Setter Property="SizeToContent" Value="Height"/>
            <Setter Property="WindowStyle" Value="None"/>
            <Setter Property="AllowsTransparency" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
    </prism:Dialog.WindowStyle>

    <Border Background="White" CornerRadius="15" BorderThickness="1" BorderBrush="#EAECF4">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.1" Color="Black"/>
        </Border.Effect>
        
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Descargar Registros" FontSize="20" FontWeight="Bold" Foreground="#4E5D78"/>
                <TextBlock Text="Seleccione el rango de fechas a descargar" FontSize="12" Foreground="#858796" Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel Grid.Row="1">
                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="Fecha Inicial (Opcional)" FontWeight="SemiBold" Foreground="#4E5D78" Margin="0,0,0,5"/>
                    <DatePicker SelectedDate="{Binding FromDate}" 
                                mah:TextBoxHelper.Watermark="Desde el inicio"
                                mah:TextBoxHelper.ClearTextButton="True"/>
                    <TextBlock Text="Si se deja vacío, descargará desde la última sincronización." FontSize="11" Foreground="#858796" FontStyle="Italic" Margin="0,3,0,0"/>
                </StackPanel>

                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="Fecha Final (Opcional)" FontWeight="SemiBold" Foreground="#4E5D78" Margin="0,0,0,5"/>
                    <DatePicker SelectedDate="{Binding ToDate}" 
                                mah:TextBoxHelper.Watermark="Hasta hoy"
                                mah:TextBoxHelper.ClearTextButton="True"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Cancelar" 
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource MahApps.Styles.Button.Flat}"
                        Margin="0,0,10,0"/>
                
                <Button Content="Descargar" 
                        Command="{Binding DownloadCommand}"
                        Background="#1CC88A"
                        Foreground="White"
                        Padding="25,8"
                        BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
